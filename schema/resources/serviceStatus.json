{
  "title": "serviceStatus",
  "description": "Describes the current status of file management service.",
  "additionalProperties": false,
  "required": ["status", "total_uploads"],
  "type": "object",
  "properties": {
    "status": {
      "description": "Current status of the file management service.",
      "type": "string",
      "enum": ["OK", "FAILURE"]
    },
    "total_uploads": {
      "description": "The mumber of active uploads.",
      "type": "integer"
    }
  }
}

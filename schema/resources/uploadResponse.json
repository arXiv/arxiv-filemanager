{
  "title": "UploadResponse",
  "description": "Describes the response of initial uploaded source package.",
  "additionalProperties": false,
  "required": ["reason"],
  "type": "object",
  "properties": {
    "reason": {
      "description": "Brief response from initial upload.",
      "type": "string"
    }
  }
}
